# Nginx configuration for Strapi CMS Admin Panel
# This routes admin.celebration-garden.com directly to Strapi admin interface
# Place this file in /etc/nginx/sites-available/strapi
# Then create symlink: sudo ln -s /etc/nginx/sites-available/strapi /etc/nginx/sites-enabled/

server {
    listen 80;
    server_name admin.celebration-garden.com;

    # Redirect HTTP to HTTPS (uncomment after SSL setup)
    # return 301 https://$server_name$request_uri;

    # For initial setup, allow HTTP
    # Routes admin.celebration-garden.com directly to Strapi (admin panel at root)
    location / {
        proxy_pass http://localhost:1337;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        
        # Increase timeouts for large uploads
        proxy_connect_timeout 600;
        proxy_send_timeout 600;
        proxy_read_timeout 600;
        send_timeout 600;
    }

    # Increase client body size for file uploads
    client_max_body_size 100M;
}

# SSL Configuration (uncomment after running certbot)
# server {
#     listen 443 ssl http2;
#     server_name admin.celebration-garden.com;
# 
#     ssl_certificate /etc/letsencrypt/live/admin.celebration-garden.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/admin.celebration-garden.com/privkey.pem;
# 
#     location / {
#         proxy_pass http://localhost:1337;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection 'upgrade';
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_cache_bypass $http_upgrade;
#         
#         proxy_connect_timeout 600;
#         proxy_send_timeout 600;
#         proxy_read_timeout 600;
#         send_timeout 600;
#     }
# 
#     client_max_body_size 100M;
# }

